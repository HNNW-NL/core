{% extends 'layouts/org.base.html.twig' %}

{# Page title meta tag #}
{% block title %}Publish project {{ id }}{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('assets/org/css/modify-project.css') }}">
{% endblock %}

{% block javascript %}
<script src="{{ asset('assets/org/js/modify-project.js') }}"></script>
{% endblock %}

{# Page content #}
{% block body %}
    <body>
    <a class="skip-link" href="#main-content">Skip to main content</a>
    <p id="unsavedBadge" class="unsaved-badge" role="status" aria-live="polite" hidden>Unsaved changes</p>
    <div class="content content--single">
        <main id="main-content">
            <section class="modify-banner reveal-up" aria-label="Modification notice" style="--reveal-delay: 60ms;">
                <p class="eyebrow">Modify</p>
                <h1>Modify Existing Project</h1>
                <p>Review the current values, then update only the fields you want to change.</p>
            </section>
            <form method="post" action="handler.php" novalidate class="reveal-up" style="--reveal-delay: 140ms;">
                <p class="form-status" role="status" aria-live="polite" hidden></p>
                <div class="modify-meta">
                    <div>
                        <label for="projectId">Choose project to modify</label>
                        <select id="projectId" name="projectId" required></select>
                    </div>
                </div>
                <section class="modify-meta" aria-label="Audit details">
                    <div>
                        <label for="lastModifiedBy">Last modified by</label>
                        <input type="text" id="lastModifiedBy" name="lastModifiedBy" value="" readonly>
                    </div>
                    <div>
                        <label for="lastModifiedAt">Current server time</label>
                        <input type="text" id="lastModifiedAt" name="lastModifiedAt" value="" readonly>
                    </div>
                </section>
                <div class="modify-grid">
                    <section class="modify-box" aria-label="Current details">
                        <h2>Current Details</h2>
                        <p class="field-help">Loaded from the database for this project ID.</p>
                        <div>
                            <label for="currentName">Current project name</label>
                            <input type="text" id="currentName" name="currentName" value="" readonly>
                        </div>
                        <div>
                            <label for="currentSummary">Current summary</label>
                            <textarea id="currentSummary" name="currentSummary" rows="3" readonly></textarea>
                        </div>
                        <div>
                            <label for="currentVisibility">Current visibility</label>
                            <input type="text" id="currentVisibility" name="currentVisibility" value="public" readonly>
                        </div>
                        <div>
                            <label for="currentStartDate">Current start date</label>
                            <input type="date" id="currentStartDate" name="currentStartDate" value="" readonly>
                        </div>
                        <div>
                            <label for="currentEndDate">Current end date</label>
                            <input type="date" id="currentEndDate" name="currentEndDate" value="" readonly>
                        </div>
                        <div>
                            <label for="currentCapacity">Current capacity</label>
                            <input type="number" id="currentCapacity" name="currentCapacity" value="" readonly>
                        </div>
                        <div>
                            <label for="currentRemotePossible">Current remote possible</label>
                            <input type="text" id="currentRemotePossible" name="currentRemotePossible" value="" readonly>
                        </div>
                        <div>
                            <label for="currentStatus">Current status</label>
                            <input type="text" id="currentStatus" name="currentStatus" value="" readonly>
                        </div>
                        <div>
                            <label for="currentPublishedAt">Published at</label>
                            <input type="text" id="currentPublishedAt" name="currentPublishedAt" value="" readonly>
                        </div>
                    </section>
                    <section class="modify-box reveal-up" aria-label="New details" style="--reveal-delay: 220ms;">
                        <h2>Updated Details</h2>
                        <p class="field-help">Only changed values will be applied.</p>
                        <div>
                            <label for="name">Project name</label>
                            <input type="text" id="name" name="name" placeholder="Project name" required autocomplete="name">
                        </div>
                        <div>
                            <label for="summary">Summary</label>
                            <textarea id="summary" name="summary" rows="3" placeholder="Short project summary" required></textarea>
                        </div>
                        <div>
                            <label for="description">Description</label>
                            <textarea id="description" name="description" rows="5" placeholder="Describe the project in detail" required></textarea>
                        </div>
                        <div>
                            <label for="visibility">New visibility</label>
                            <select id="visibility" name="visibility" required>
                                <option value="public">Public</option>
                                <option value="private">Private</option>
                                <option value="unlisted">Unlisted</option>
                            </select>
                        </div>
                        <div>
                            <label for="startDate">Start date</label>
                            <input type="date" id="startDate" name="startDate">
                        </div>
                        <div>
                            <label for="endDate">End date</label>
                            <input type="date" id="endDate" name="endDate">
                        </div>
                        <div>
                            <label for="capacity">Capacity</label>
                            <input type="number" id="capacity" name="capacity" min="1" placeholder="Max participants">
                        </div>
                        <div>
                            <label for="remotePossible">Remote possible</label>
                            <input type="checkbox" id="remotePossible" name="remotePossible" value="1">
                        </div>
                        <div>
                            <label for="statusId">Status</label>
                            <select id="statusId" name="statusId">
                                <option value="1">Draft</option>
                                <option value="2">Active</option>
                                <option value="3">Closed</option>
                            </select>
                        </div>
                    </section>
                </div>
                <div class="modify-meta">
                    <div>
                        <label for="effectiveAt">Effective date and time</label>
                        <input type="datetime-local" id="effectiveAt" name="effectiveAt">
                    </div>
                </div>
                <div class="button-row">
                    <button type="submit" name="intent" value="modify">Modify project</button>
                    <button type="submit" name="intent" value="delete" class="button-danger" formnovalidate>Delete project</button>
                </div>
            </form>
        </main>
    </div>
    <dialog id="deleteConfirmDialog" class="delete-dialog" aria-labelledby="deleteDialogTitle">
        <form method="dialog" class="delete-dialog__form">
            <h2 id="deleteDialogTitle">Confirm permanent deletion</h2>
            <p>Type <strong>DELETE</strong> to permanently remove this project. This cannot be undone.</p>
            <label for="deleteConfirmInput">Confirmation</label>
            <input id="deleteConfirmInput" type="text" autocomplete="off" placeholder="Type DELETE">
            <p id="deleteConfirmHint" class="field-help">Type DELETE (all caps) to enable the button.</p>
            <div class="delete-dialog__actions">
                <button value="cancel">Cancel</button>
                <button id="deleteConfirmButton" value="confirm" class="button-danger" disabled>Delete permanently</button>
            </div>
        </form>
    </dialog>
    </body>
{% endblock %}
