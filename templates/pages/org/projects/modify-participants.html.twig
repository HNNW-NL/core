{% extends 'layouts/org.base.html.twig' %}

{# Page title meta tag #}
{% block title %}Modify project {{ id }} participants{% endblock %}

{# CSS #}
{% block stylesheets %}{% endblock %}

{# JavaScript #}
{% block javascript %}{% endblock %}

{# Page content #}
{% block body %}
    <h2>Projects - Modify Participants</h2>
    <p>Organization panel - modify project ({{ id }}) participants page.</p>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Role</th>
                <th>Start date</th>
            </tr>
        </thead>
        <tbody>
        {#  { % for participant in participants % }
           <tr>
               <td>{{ participant.accounts.username }}</td>
               <td>
                   <select>
                       {% for role in roles %}
                           <option value="{{ role.id }}" {% if role.id == participant.role.id %}selected{% endif %}>
                               {{ role.name }}
                           </option>
                       {% endfor %}
                   </select>
               </td>
               <td>{{ participant.joinedAt|date('Y-m-d H:i') }}</td>
           </tr>
        {% endfor %} #}
        </tbody>
    </table>
{% endblock %}
