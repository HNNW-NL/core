{% extends 'layouts/org.base.html.twig' %}

{# Page title meta tag #}
{% block title %}Modify project {{ id }} work packages{% endblock %}

{# CSS #}
{% block stylesheets %}{% endblock %}

{# JavaScript #}
{% block javascript %}{% endblock %}

{# Page content #}
{% block body %}
    <h2>Werkpakketten beheren</h2>
    <p>Project: {{ project.title }}</p>

    <form method="post">
        <div>
            <label for="title">Titel</label>
            <input id="title" type="text" name="title" required>
        </div>

        <div>
            <label for="description">Beschrijving</label>
            <textarea id="description" name="description"></textarea>
        </div>

        <button type="submit">Werkpakket koppelen aan project</button>
    </form>

    <hr>

    <h3>Gekoppelde werkpakketten</h3>

    {% if workPackages is empty %}
        <p>Er zijn nog geen werkpakketten gekoppeld aan dit project.</p>
    {% else %}
        {% for workPackage in workPackages %}
            <div style="border: 1px solid #ccc; padding: 15px; margin-bottom: 15px;">
                <h4>{{ workPackage.title }}</h4>
                <p>{{ workPackage.description }}</p>

                <form method="post" action="{{ path('org.modifyProject.workPackages.delete', {
                    projectId: id,
                    workPackageId: workPackage.id
                }) }}">
                    <button type="submit">Verwijderen</button>
                </form>
            </div>
        {% endfor %}
    {% endif %}
{% endblock %}
